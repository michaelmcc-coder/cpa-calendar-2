import './globals.css'
export const metadata = { title: 'CPA Staff Calendar', description: 'Office display for CPA faculty deadlines and reminders' }
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body>{children}</body></html> }
